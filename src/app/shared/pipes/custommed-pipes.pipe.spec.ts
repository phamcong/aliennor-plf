import { CustommedPipesPipe } from './custommed-pipes.pipe';

describe('CustommedPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CustommedPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
