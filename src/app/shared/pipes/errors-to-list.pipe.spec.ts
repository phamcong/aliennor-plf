import { ErrorsToListPipe } from './errors-to-list.pipe';

describe('ErrorsToListPipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorsToListPipe();
    expect(pipe).toBeTruthy();
  });
});
